To properly run the aplication, you need to have installed vagrant and virtualbox, and run this on a virtual machine.
Run the database_setup.py to configure the database
Then, run server.py, and then the server will be ready to interact. Go to localhost:8001 and there the website will be displayed